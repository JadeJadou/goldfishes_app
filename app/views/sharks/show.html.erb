<div class="container mt-3">
  <h2><%= @shark.name %></h2>
    <% if @shark.photos.attached? %>
    <%= cl_image_path(@shark.photos.key) %>
    <% else %>
       <img src="https://images.unsplash.com/photo-1544552866-d3ed42536cfd?q=80&w=2942&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D" width=300px height=10% alt="">
    <% end %>


  <p><%= @shark.description %></p>
<div style = "display: flex; justify-content: flex-end">
  <div class = "card-simple">
    <%= @shark.price_per_day %> € par jour

  <%= simple_form_for(@booking) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :start_date, html5: true %>
    <%= f.input :ending_date, html5: true %>

  </div>

  <div class="form-actions">
    <%= f.button :submit, class: "button-frozen"%>
    <% end %>
  </div>
</div>

</div>
